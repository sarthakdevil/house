import { View, Text, StyleSheet } from 'react-native'
import React from 'react'

export default function Circles({position}) {
  return (
    <View style={styles.circle} />
  )
}
const styles = StyleSheet.create({

  });
  